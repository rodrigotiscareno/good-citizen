// These keys have already been rotated and must be replaced before deployment.

export let config = {
  host: "aws_host",
  user: "user",
  password: "password",
  database: "database",
  multipleStatements: true,
};

module.exports = config;
