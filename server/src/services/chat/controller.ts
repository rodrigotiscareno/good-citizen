import axios from "axios";

const TOKEN = "OPEN_AI_TOKEN";

const getChatInstructions = (mood: string, answerLength: string) => {
  // The protect the team's business interests, this code block has been redacted and removed from the public repo.

  const chatInstructions = ["instruction 1", "instruction 2"];
  return chatInstructions;
};

export const chatFakeResponseToNotGoBroke = async (
  numIt: number
): Promise<any> => {
  // The protect the team's business interests, this code block has been redacted and removed from the public repo.
};

export const chatTest = async (): Promise<any> => {
  // The protect the team's business interests, this code block has been redacted and removed from the public repo.
};

export const chatPrompt = async (
  messages: any,
  mood: string,
  answerLength: string
): Promise<any> => {
  if (!messages) {
    return Promise.reject("No messages");
  }

  // The protect the team's business interests, this code block has been redacted and removed from the public repo.
};

export const getCategory = async (
  text: string,
  categories: string[]
): Promise<any> => {
  // The protect the team's business interests, this code block has been redacted and removed from the public repo.
};
